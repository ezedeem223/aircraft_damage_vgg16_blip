# Notebooks

The notebooks in this directory are preserved for provenance and exploration.

The maintained runtime surface of this repository is the packaged Python
project under `src/aircraft_damage`, together with the config-driven scripts
and demo entrypoints.

The root `.gitattributes` file marks `notebooks/**` as `linguist-vendored` so
archived notebook content does not define GitHub's language classification for
the maintained project.
